{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "node packages/cli/dist/main.js run --ai --channels --telemetry --dashboard",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
